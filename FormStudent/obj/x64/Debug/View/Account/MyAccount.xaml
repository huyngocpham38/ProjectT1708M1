<Page x:ConnectionId='1'
    x:Class="FormStudent.View.MyAccount"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FormStudent.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:FormStudent.Entity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <ListView x:ConnectionId='2'                                    x:Name="MyListSong">
                <ListView.ItemTemplate>
                    <DataTemplate                           >
                        <StackPanel x:ConnectionId='3' Margin="0, 0, 0, 10" Orientation="Horizontal"  Tag="{Binding}"  Width="300" >

                            <StackPanel Margin="10,15,0,0">
                                <TextBlock x:ConnectionId='4'                           Style="{ThemeResource BaseTextBlockStyle}"/>
                                <TextBlock x:ConnectionId='5'                          Style="{ThemeResource BaseTextBlockStyle}"/>
                            </StackPanel>

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>

